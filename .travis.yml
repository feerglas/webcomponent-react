language: node_js
node_js:
 - "stable"

cache:
  directories:
  - node_modules

script:
  - npm run build

deploy:
  provider: heroku:git
  api_key: travis encrypt $HEROKU_API_KEY
  edge: true # opt in to dpl v2
  local_dir: build
  on:
    branch: master
